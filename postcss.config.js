module.exports = {
  // PostCSS is a tool for transforming CSS with JavaScript plugins.
  // Tailwind CSS uses PostCSS to process your CSS and generate the final stylesheet.
  plugins: {
    // 'tailwindcss' plugin processes your CSS to include Tailwind's utility classes.
    tailwindcss: {},
    // 'autoprefixer' automatically adds vendor prefixes to CSS rules for browser compatibility.
    autoprefixer: {},
  },
};
